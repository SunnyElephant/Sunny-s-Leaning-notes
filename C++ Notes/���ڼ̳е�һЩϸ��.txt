1.可以将派生类的对象赋给基类，但是不能将基类对象赋给派生类对象（计时工都是员工，而员工不一定是计时工）
2.基类的指针可以指向派生类，而派生类的指针不能指向基类。

3.基类的保护成员在后辈中一直是保护成员。

4.要重定义成员函数，必须在派生类定义中列出它的声明，并且这个声明与基类的完全相同。

5.构造函数，析构函数，私有成员函数，重载赋值操作符（=）是不会被继承的。
在派生类中重新激活赋值操作符：
Derived& Derived::operator =(const Derived& right_side)
{
	base::operator=(right_side)				//等于调用了基类的赋值操作符
}

在派生类中