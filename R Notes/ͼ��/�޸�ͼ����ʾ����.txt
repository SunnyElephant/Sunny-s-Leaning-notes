par()函数可以查看当前的图像显示参数
par(...,...)可以修改当前的图像显示参数
当修改了参数后，直到改回来，或者结束会话之前，一直有限


参数：
pch:制定绘制点时使用的符号
cex:指定符号的大小。cex是一个数值，表示绘图符号相对于默认大小的缩放倍数。默认大小为1,1.5表示放大为默认值的1.5倍，0.5表示缩小为默认值的50%。
lty:指定线条类型
lwd:指定线条宽度。lwd是以默认值的相对大小来表示的（默认为1）。



example:
> par(lty=10,pch=17)
> plot(dose,drugA,type="b")

